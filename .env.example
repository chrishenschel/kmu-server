# =============================================================================
# KMU Server – environment variables
# =============================================================================
# This file is a REFERENCE only. Do not copy it to .env with real secrets
# committed. 02-system-setup.sh creates .env and fills all values below.
# Add any new variables your deployment needs at the end.
# See docs/ENV-REFERENCE.md for full descriptions.
# =============================================================================

# --- Domain and initial admin (replace with your values if setting by hand) ---
DOMAIN=example.com
USERNAME=admin
USERFULLNAME="Admin User"
PASSWORD=change-me

# --- PostgreSQL (setup generates random PG_PASS) ---
PG_PASS=

# --- Authentik (setup generates random secrets) ---
AUTHENTIK_SECRET_KEY=
AUTHENTIK_BOOTSTRAP_PASSWORD=
AUTHENTIK_BOOTSTRAP_TOKEN=
AUTHENTIK_BOOTSTRAP_EMAIL=hostmaster@example.com

# --- LDAP for Stalwart (setup sets LDAP_BASE_DN; LDAP_OUTPOST_TOKEN after blueprints) ---
LDAP_BASE_DN=dc=example,dc=com
LDAP_OUTPOST_TOKEN=

# --- Matrix/Synapse OIDC (setup generates) ---
MATRIX_CLIENT_ID=
MATRIX_CLIENT_SECRET=

# --- Nextcloud OIDC (setup generates) ---
NC_CLIENT_ID=
NC_CLIENT_SECRET=

# --- Nextcloud outgoing mail (setup creates noreply user and sets these) ---
NOREPLY_EMAIL=noreply@example.com
NOREPLY_MAIL_PASSWORD=

# --- Vaultwarden SSO (setup generates) ---
VW_CLIENT_ID=
VW_CLIENT_SECRET=
# VW_SIGNUPS_ALLOWED=true
# VW_SSO_ONLY=false

# --- Immich OAuth (setup generates) ---
IMMICH_CLIENT_ID=
IMMICH_CLIENT_SECRET=

# --- Jitsi Meet (setup generates JWT and sets MEET_DOMAIN) ---
JWT_APP_ID=jitsi
JWT_APP_SECRET=
MEET_DOMAIN=meet.example.com

# --- Optional overrides (defaults shown) ---
# RESTART_POLICY=unless-stopped
# JITSI_IMAGE_VERSION=stable
# CONFIG=./jitsi/config
# JVB_PORT=10000
# JICOFO_REST_PORT=8888
# JVB_COLIBRI_PORT=8080
# XMPP_PORT=5222
# PROSODY_S2S_PORT=5269
# PROSODY_HTTP_PORT=5280
